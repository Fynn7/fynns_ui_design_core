---
description: @fynns/ui consumer hard contract — paste into any app that consumes the design system
alwaysApply: true
---

# @fynns/ui 消费仓硬性条约（复制即用）

本文件是**消费仓**侧对 `@fynns/ui` 的补充合同。权威细节：
`docs/DESIGN_SYSTEM.md`、`llm/CONSUME.md`、`llm/PERF.md`。失败模式 slug 索引：
`llm/CONSUMER_TREATY.md`（细节 → Hard rules + sandbox，勿把 AGENTS 当 SoT）。

**可见 UI 标点（硬）：** 强烈不推荐中间点 **`·`** 与破折号 **`—`**（装饰性
**`–`** 当字段粘合符同禁）——含 Card / Collapsible `title`、FieldBlock 标签、List
文案、目的地名。标题 = 人会说的短分区名；计数 / 字数 / token / 路径 → 正文
`.fynns-table-meta`（`#card`）。组织 → `supportingText`，日期 →
`trailingSupportingText`（`2025-10 - 2026-03`）。禁止 `Org · dates`。空 trail 用
ASCII `-`。

## 安装与 API

1. **零 token 日常消费（默认）**：`@fynn7/ui-design-core` 来自同级
   `../fynns_ui_design_core` 的 **`file:`**；Vite/tsconfig `@fynns/ui` →
   `node_modules/@fynn7/ui-design-core/src/index.ts`。禁止要求
   `NODE_AUTH_TOKEN` / Packages 登录才能 install/dev；**.npmrc 禁止**提交
   authToken 行；sibling 用 `@fynn7:registry=https://registry.npmjs.org`。禁止
   git submodule 日常消费。
2. **UI 工作前核安装**：sibling + `file:`；core
   `ensure-sibling-ui-core.mjs --target <根> --install --npmrc --update` 与/或
   `npm run consume:check -- --target <根>`。`consume:install` 接线
   `fynns-ui:gate` 到 predev/prebuild/postinstall。脏 sibling → 软跳过同步；缺
   导出仍硬失败。CI：`FYNNS_UI_STRICT_SIBLING_SYNC=1`。E401 → 切回 sibling。见
   `llm/CONSUME.md`。
3. Vite：`resolve.dedupe: ["react", "react-dom"]`；`tsconfig` ≥ **ES2022**。
4. **只走公开 API**：props / children / 本地文案。禁止本地重写 `.fynns-*`、禁止
   `@radix-ui/*` / `sonner`、禁止复活已 purge API。缺能力 → 先在 core 加，再刷新
   sibling / bump。
4b. **Keep-set 铬细节禁止自改（硬）**：禁止在消费仓「修」Select / SearchBar /
   Autocomplete / DropdownMenu / **Input field-shell** 的圆角 / padding / shadow /
   overflow / 展开方位（含 `.fynns-select` / `.fynns-search-bar*` / `.fynns-menu` /
   `.fynns-field-shell` / `.fynns-field-affix`）。对照 `#select` / `#menu` /
   `#password` / `#input`；不对 → **只**在 core 约束。
4c. **Select / Autocomplete 已软废弃（硬）**：新 UI 勿用。离散选项 →
   `DropdownMenu`（`#menu`）；可筛选 / 命令式 → `SearchBar` / `CommandPalette`。
   现有命中 → 推荐迁移。例外：Pagination 每页条数可暂留 stock Select。
5. 样式只用 `var(--fynns-*)`。正文/chrome `--fynns-font-ui`；代码
   `--fynns-font-mono`；禁止主界面 serif。
6. 瞬时反馈：`snackbar` + `<SnackbarHost />`。

## 加载与忙碌（硬）

| 场景 | 用 | 禁止 |
| --- | --- | --- |
| 全屏阻塞 | `BusyScrim` | EmptyState + 圈 |
| 栏目冷启动 | `BusyRegion` `fill`（FillColumn / shell / PageScroll→content-column） | `fill` 塞 unit-stack / Card；FieldHint + busy 同井；高度链断 |
| Dialog/Card 正文加载 | `BusyRegion`（定高才 `fill`） | 裸 `CircularProgress`；busy 内叠 Select/Pagination |
| 刷新已有表面 | BusyRegion 只包 List/表 | 整 Card（含脚栏）包进 busy |
| 一宿主进度 | 环 **或** 条（linear+value） | 双进度；BusyRegion + chrome `loading` 同叠 |
| Hang / 超时 | `runBusyTask`/`runLoadingTask` + `timeoutMs`/`signal`/`onError` → **先清 busy** 再错误表面 | 永久 BusyRegion/Scrim/`loading`；空数据当 loading |
| BusyRegion 遮罩 | core `--fynns-color-busy-region-mask` + 轻 blur；空冷启动无 children → 无 wash | 私造 surface/scrim；复用 BusyScrim overlay；空区贴图色块 |
| 栏目冷启动失败 | 清 busy → `InlineAlert` + hint + 末端 Retry | 永久 fill；EmptyState 当失败壳；静默空态 |
| 错误表面 | 瞬时 → snackbar；面板 → InlineAlert+Retry；生成 → `ChatMessage.error`+`onRetry`；机器码留消费方 `AppError` | snackbar 代替面板恢复；EmptyState 当 500；码塞进 `@fynns/ui` |

对照 `#busy-region` / `#sandbox-pane-load-error` / `#busy-paint` /
`#sandbox-busy-task-timeout`。

## Shell（硬）

- 绿田默认 **`DestinationAppShell`**：目的地 = 可调宽抽屉 **或** `hidden` —
  **禁止**窄屏 densify 成 unlabeled `NavigationRail`。
- **`nav` = 目的地 only**；正文 → `children`；检查器 → `EndAside`。“Clipped” =
  M3 拓扑，不是文字裁切。
- Settings：**仅** `navFooter` 齿轮；体 = 软件偏好。功能配置走独立目的地。语言
  开关在 Settings 内 `ToggleGroup` — **禁止** TopAppBar。Footer `Avatar`
  `name` = **可见**账户标签全文 — **禁止**另传截断名。对照 `#layouts-demo-shell`
  / `#avatar`。
- 模式侧栏偏好：单行 `ControlRow` + `InfoHint` `sm` + 仅轨道 `Switch` — 禁止
  `ControlBlock` 长 description。标签左缘对齐 Item pad；`--toolbar-end` 主新建
  Plus / Switch 与目的地 Item pill **共右端** — 禁止再套 `item-pad-end`。
  `--toolbar-end` IconButton 一律 **sm** **32dp**。body 直接子间距 =
  `search-gap` **8dp**。对照 `#layouts-demo-navigation-drawer`。
- SyncSideFilter：`ToggleGroup` `compact` `fullWidth` + `showCheck={false}` +
  省略 option `tip`；短可见标签。对照 `#layouts-demo-navigation-drawer` /
  `#toggle-group`。
- 模式目录加载失败：短 `InlineAlert` + `InfoHint` `tone="danger"` + 末端 Retry —
  禁止长后端文塞进 `message`。对照 `#sandbox-navdrawer-mode-catalog-fail`。
- `EndAside`：只切 `open` — 禁止 `{open && <EndAside>}`；toggle 勿触发 workspace
  BusyRegion boot。窄检查器：一主 CTA + IconButton/菜单。对照
  `#layouts-demo-shell`。

## 常见失败模式（slug 索引）

其余细节 → `llm/CONSUMER_TREATY.md` + `docs/design-system/hard-rules.md` +
sandbox `#anchor`。

- **sandbox-only aesthetics** — 勿只改 sandbox overrides；token 进 `tokens.ts`。
- **squashed drawer / icon-only rail densify** — 拥挤 → `hidden`，勿假 rail。
- **drawer sheet headline under TopAppBar** — DestinationAppShell 勿传 `drawerHeadline`。
- **hub-split / catalog edit as silent PageScroll** — 列表保持挂载；编辑 → `Dialog` `lg` + `showCloseButton`。
- **drill-in / mode sidebar hard-swap** — 换 body 须 `navKey` + `navDirection`；退出 `"back"`；禁止硬切。`#layouts-demo-drill-in`
- **fat Surface / Card per catalog row** — 路径/书签/配方 → 一个 `List` of `ListItem`；禁止每项胖 Card。`#list` / `#sandbox-list-recipe-catalog`
- **org · dates / UI · — punctuation / Card title · meta** — 槽位分开；禁用 `·`/`—` 粘合。`#card` / `#list`
- **trailingSupportingText right-hug drift** — 日期目录须 `trailingMetaAlign="start"`；状态/kind → `overline`。`#list` / `#timeline`
- **Timeline row hover edit/delete** — 编辑在 Dialog 脚栏；勿 rail `--with-end` 铅笔/垃圾桶。`#timeline`
- **NavigationDrawerItem badge / trash / no more-menu** — 行动作用 `trailing` ghost sm 或 More+`DropdownMenu`（删 `tone="danger"`）；禁止删进 `badge`。`#layouts-demo-navigation-drawer`
- **mode drawer toolbar trash+new twin** — 会话用 `NavigationDrawerNewChat`；可选 trailing More 放全部删除；禁止 Trash+Plus 并排。`#sandbox-navdrawer-session-chrome`
- **chat product / aside host wrong tree** — 会话：`ClippedNavShell`+drawer+`FillColumn`→`Chat`；侧栏主题：`DestinationAppShell`+`EndAside`→`.fynns-chat-host--fill`→`Chat`。禁止 PageScroll 当 chat 主滚、composer 进 FillColumn `footer`。`#layouts-demo-chat-product` / `#layouts-demo-chat-aside`
- **FillColumn guide Surface flush to Chat** — 短 soft Surface / 预览 → `header`；Chat → `children`；禁止 `children` 兄弟 + `paddingBottom:0` 贴死 ChatActivity。`#layouts-demo-fill-column` / `#sandbox-fill-column-guide`
- **PageScroll mid-scroll hard clip** — 只用 keep-set `PageScroll`（渐隐已封装）；禁止裸 `overflow:auto`。`#page-scroll`
- **FillColumn header mid-scroll hard clip** — canvas 封顶的 `header` 渐隐已封装（≥ **0.5.278**）；禁止私有 `mask-image`。Header **content-sized**（`flex: 0 0 auto` ≥ **0.5.279**），勿靠 flex-shrink 把短 guide Surface 压出嵌套滚动条。`#sandbox-fill-column-guide`
- **overlay scrollbar above / buried under Select/Menu** — 页轨 z 在 modal-flyout 下；飞层轨用 scroll-overlay-flyout。`#sandbox-scroll-menu-stack`
- **mode drawer tools↔filter crushed / toolbar-end md** — body 直接子间距 8dp；toolbar-end 盘 sm 32dp。`#layouts-demo-navigation-drawer`
- **Dialog foot Delete leftmost / exit clears title** — Cancel 左、主确认右；`open→false` 同拍勿清空 title/body。`#sandbox-list-recipe-catalog`
- **Dialog title top ≠ body-end IconButton** — 居中非 Confirm head 顶距 = `content-inset`（18dp，≥ **0.5.280**），与 body 底 / 尾部主 `IconButton` 下距齐；勿改 `.fynns-dialog-head` 或半 `dialog-inset`。`#overlays`
- **BottomSheet title top ≠ actions bottom** — header 外缘顶距 = `sheet-actions-pad-bottom`（24dp，≥ **0.5.281**）；勿把 `sheet-header-gap` 当外缘。`#overlays`
- **FillColumn header top-only inset** — Destination canvas header 块向等量 `dialog-inset`（≥ **0.5.281**）。`#sandbox-fill-column-guide`
- **twin loading rings / BusyRegion + chrome loading / Card batch + row twin** — 一簇最多一圈；busy 时 chrome 只 `disabled`；批量只留 Card 主 CTA。`#sandbox-list-repo-path-actions`
- **IconButton busy swaps CircularProgress** — 用 stock `loading`；禁止嵌套 `CircularProgress`。`#icon-button`
- **runBusyTask hang / Confirm trap / empty-as-busy** — 须 `timeoutMs`/`signal`/`onError`；Confirm 用 `runLoadingTask`+`onAbort`；ChatComposer `busy` 须 `onStop`。
- **section-body IconButton sm / ControlRow unsolicited item count** — 段级只用 md ghost+Tooltip；顶栏禁止裸 `{n}`/`(N)` 塞动作簇。`#rhythm` / `#sandbox-rhythm-catalog`
- **provider Manage flat dump** — ControlRow → 密码 FieldBlock + 同行 Input `__grow` + Save `IconButton` ghost sm → 模型 `DropdownMenu`+刷新 → Verify；禁止 List 墙/Chip/Dialog 代密钥。`#provider-settings`
- **ChatActivity tool-icon / artifact default** — 默认 status mark + 连续轨（done = accent；pulse 时 node 洗底挡住轨）；勿默认扳手/文件图标或强制 Artifact。`#activity`
- **service Chip+label / ControlRow left-packed / label crush** — 状态只在 `label`；簇默认右齐；长 path meta 省略+InfoHint。`#rhythm`
- **FieldHint restates ToggleGroup / TopAppBar InfoHint / overline restates trailing** — 禁止同源复述。`#rhythm` / `#layouts-demo-shell` / `#sandbox-list-repo-path-actions`
- **private labeled Button cluster gap / Select refresh crammed** — 用 `.fynns-control-cluster`（+ `--end-align`/`__grow`）。`#rhythm`
- **env key FieldHint / Input trailing md / affix far from edge** — 门户文案用标签行 InfoHint；壳内盘一律 `IconButton` `sm`；禁止负 margin/壳外克隆 eye。`#password` / `#input` / `#env-check`
- **InlineAlert beside List / Banner icon top-pinned** — 告警全宽兄弟在上；多行 Banner icon/X 垂直居中。`#banner`
- **EndAside instant open / dense labeled / Card flush bottom** — 切 `open`；`label=""`+一主 CTA；PageScroll 内 Card 勿贴窗格底。`#layouts-demo-shell`
- **PageScroll Collapsible / Textarea file body / FillColumn megacard / Card sheet-max** — 短表单走 `#form-recipe`；有扩展名用 `CodeBlock`；内容列铺满页滚，禁止 sheet/chat/dialog-max。`#page-scroll`
- **long Table/List dumps all rows** — Table `RevealMore` 10/10；List 5/5。`#table` / `#list`
- **Pagination overlay / option repeats noun / gaps crushed** — stock Select；options 仅数字；间距 8/16dp。`#pagination`
- **Select fullWidth / menu width / supporting kisses** — 铺满用 `fullWidth`/`__grow`；菜单 width=live shell；hint 用 `field-hint-gap`。`#select` / `#sandbox-select-wide-short`
- **DropdownMenu FieldBlock width / chevron / panel shift / chrome clip** — matchTriggerWidth；core 注入 chevron；禁止消费仓 left/transform/translateY。`#menu` / `#sandbox-menu-field-match`
- **truncated option / chrome label lacks OverflowTip** — 溢出须 `…`+Tooltip 全文；禁止 `title=`。`#select` / `#card` / `#list`
- **consumer restyles keep-set chrome / Select Autocomplete deprecated** — 铬改只在 core；新屏用 Menu/SearchBar。`#select` / `#menu`
- **outcome Chip as status / List path Switch+Chip+danger soup** — 结果标用 `.fynns-list-item-status`；目录 trailing 仅 ghost md 簇。`#list` / `#sandbox-list-repo-path-actions`
- **List scroll-well meta/rail / spill / underlap / focus ring crack** — 勿私造 end pad/overflow/z-index。`#sandbox-list-repo-path-actions` / `#sandbox-list-status-action`
- **FieldStack Grid centers / hugs max-content** — Grid `align-items: start`；列铺满父级；勿消费仓改回 center。`#sandbox-field-stack-grid-select` / `#form-recipe`
- **Card chrome labeled Copy/Save / FieldBlock Save wraps / primary leftmost** — 自明动作用 `IconButton`+Tooltip；Save 与 Input 同行 ghost sm；primary 盘 LTR 末位。`#sandbox-card-chrome-icon-actions` / `#provider-settings` / `#sandbox-card-head-primary-end`
- **`#rhythm` morph + twin strips** — 禁止 PageScroll 上两条同构 ControlRow 节名+md 簇。`#sandbox-rhythm-catalog`
- **Chat empty starters as Chip** — 空线程 starter 用满宽可点 `Surface` soft/padded/interactive；勿 Chip / 勿复活 `ChatStarterPrompts`。`#chat`

## 表单与密度（短）

- 语义分组用 **`FieldStack`** + kind 跳跃时 `Divider`。对照 `#form-recipe`。
- 工具条：`ControlStack` / `ControlRow` / `.fynns-control-cluster` /
  `ControlBlock` `description`。对照 `#rhythm`。
- List 目录 kind → leading 图标 + meta；禁止 start tick / Chip 当状态。结果标 →
  `.fynns-list-item-status`。多指标 trailing → `.fynns-list-item-trailing-stats`。
- Card/Collapsible 嵌套表面子级 → `chrome="plain"`（plain ≠ flush）。
- PageScroll 分区 Card：勿用 sheet/chat/dialog-max 收窄；内容列铺满页滚；与上方
  ControlRow 同列宽。FullscreenDialog 设置栈亦横向铺满。
- 长 Table：`useRevealMore` + `RevealMore`（10/10）。宽表横向溢出：默认纵向滚轮
  → `scrollLeft`；关闭 `data-fynns-wheel-x="off"`。对照 `#table`。
- 长 List：`useRevealMore` + `RevealMore`（**5**/5）。对照 `#list`。

## Chat（短）

主栏气泡列宽 / composer：`llm/CHAT_COMPOSER_LAYOUT.md`。ARIA：
`llm/CHAT_ARIA_PARITY.md`。优先 `ChatMessage markdown` / `ChatMarkdown`。
产品壳默认：`ClippedNavShell` + 会话 `NavigationDrawer`（`NavigationDrawerNewChat`
…）+ `FillColumn` → `Chat`（`#layouts-demo-chat-product`）；扁平根仍用
`DestinationAppShell`。新会话：问候 + text starter + composer 进
`ChatThread.empty` 居中；有消息后 composer 贴 `ChatThread` 下。空线程 starter：
满宽可点 `Surface` soft/padded/interactive（M3 Shared Axis Y 轮播；勿 Chip /
勿 `ChatStarterPrompts`）。对照 `#chat`。勿私有 CSS「修」body stack gap。

## 性能

密集 inspector / shell / token GUI → 先读 `llm/PERF.md`（禁止 measure probe
塞进 `ClippedNavShell` MutationObserver 子树）。

## 何时停手

keep-set 不够 → **明确告知用户**必须先改 `fynns_ui_design_core`，再 bump
消费仓。禁止 `node_modules/@fynn7/ui-design-core` 内改功能。
