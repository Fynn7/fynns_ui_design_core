# AGENTS.md — <项目名>

<!--
目标：常驻 300–450 tokens。超过 ~50 行就说明有东西该下沉了。
只放「不知道就会写错代码 / 破坏数据 / 提交不该提交的东西」的内容。
不要复制 README —— 和 README 重复的内容会实测拉低 agent 表现。
-->

<一两句话说清这个仓库是什么、产出什么。不写背景故事。>

## 目录地图

| 路径 | 用途 |
|------|------|
| `src/…` | |
| `tests/…` | |

<!-- 只列 agent 真的需要导航的路径，5–7 行封顶。 -->

## 验证

```
<单文件测试命令>
<全量测试命令>
<typecheck / lint 命令>
```

<!--
这是整个文件里投入产出比最高的一节。
缺了它，agent 要么不验证，要么猜一个错的命令然后报告"测试通过"。
-->

## 硬约束

1. <违反会造成实际损害的规则，一条一行，祈使句>
2. …
8. 勿 git commit / push，除非用户明确要求。

<!--
每条必须能独立成立。一条超过两行就说明它其实是一份契约文档，该下沉。
反例：把外壳架构 + 生命周期 + 组件选型 + API 纪律塞进同一个编号项。
-->

## 何时读长文

| 需要 | 打开 |
|------|------|
| <某类任务> | [`docs/<文档>.md`](docs/<文档>.md) |
| 术语与命名（写文档 / UI 文案时） | [`docs/GLOSSARY.md`](docs/GLOSSARY.md) |

<!--
这张表是分层的关键：它让「下沉」变成「按需可达」而不是「丢失」。
每一行要让 agent 能判断「我现在这个任务需不需要打开它」。
写「架构说明」不行，写「改聊天主壳 / composer / 消息渲染时」才行。
-->
